source 'https://rubygems.org'

# Ruby version
ruby ">= 2.7.0"  # Or upgrade to Ruby 2.7.0 or later

# Exclude problematic versions of cocoapods and activesupport that causes build failures.
gem 'cocoapods', '>= 1.16.2'
gem 'activesupport', '>= 6.1.7.5', '!= 7.1.0'  # Make sure this version is compatible with Ruby 2.7.0+
gem 'xcodeproj', '>= 1.27.0'  # Adjust xcodeproj version to meet cocoapods requirements

# Ruby 3.4.0 has removed some libraries from the standard library.
gem 'bigdecimal'
gem 'logger'
gem 'benchmark'
gem 'mutex_m'

# Make sure to use Ruby 2.7.0+ if needed
ruby ">= 2.6.10p210"
